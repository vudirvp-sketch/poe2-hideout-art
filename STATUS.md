# STATUS.md

Текущее состояние проекта `poe2-hideout-art`. Короткая живая памятка для
агента/человека: что работает, что сломано, что делать дальше. Длинную
историю изменений не ведём — для этого есть `CHANGELOG.md`.

## Что работает (проверено 2026-07-09, версия 0.2.3)

- Парсер `.hideout` (толерантный, регэксп, сохраняет дубликаты ключей).
- Врайтер (байт-совместимый, дубликаты ключей сохраняет).
- Геометрические трансформации: `shift`, `rotate`, `mirror_x`, `mirror_y`,
  `recombine`.
- Превью в PNG (matplotlib, по одной точке на размещение).
- Конвейер `img2hideout`:
  - базовый режим (4-цветная палитра, без dither, без alpha) совместим
    с 0.1.0 побайтово;
  - alpha-канал PNG — пиксели с alpha < порога пропускаются автоматически;
  - 3 цветовых метрики: `rgb`, `weighted`, `redmean`;
  - Floyd-Steinberg dithering (опционально);
  - jitter `r` (кратный 15°) и `variant` (опционально, с seed);
  - `step` ИЛИ `tile_size` — размещение каждые N пикселей / один тайл;
  - `bounds` — отсечение по мировому прямоугольнику **или** именованный
    шорткат (`--bounds canal` для Canal Hideout);
  - `resample` — nearest/bilinear/bicubic/lanczos;
  - `--preview` в CLI — превью PNG рядом с выходным файлом.
- CLI: `inspect`, `layers`, `stats`, `preview`, `shift`, `transfer`,
  `img2hideout`.
- Каталог хешей: **46 известных декораций** (11 функциональных + 8 NPC +
  4 исходных art + 5 тёплых Maraket + 18 новых Canal из «исходники/»).
- Геометрия Canal Hideout: `CANAL_HIDEOUT_HASH = 60415`,
  `CANAL_HIDEOUT_BOUNDS = (700, 540, 860, 775)` — bounding box играбельного
  полотна, вычисленный по 11 Cordilina-размещениям пользовательской
  разметки границ.
- **Каталог размеров декораций (0.2.3):** `DECORATION_FOOTPRINT_CATALOG`
  в `constants.py` — для каждой из 27 art-декораций хранит число
  наблюдаемых размещений, мин/медианную дистанцию между соседними
  размещениями и оценку размера в тайлах с указанием confidence
  (high/medium/low/single/none). Atziri Statue и прочие функциональные
  объекты намеренно исключены (по запросу пользователя: огромные, в
  композиции не участвуют).
- Тесты: **233 pytest-теста** (232 проходят, 1 skipped — требует старую
  папку `upload/` с экспортом «Береговая галька», которого нет в репе).

## Известные проблемы (Known Issues)

Все ранее задокументированные KI-1..KI-9 закрыты. См. `CHANGELOG.md` для
истории. Краткое резюме:

| KI  | Статус     | Что было                              |
|-----|------------|---------------------------------------|
| KI-1 | fixed 0.2.1 | `tile_size` auto-calibrates `step` |
| KI-2 | partial 0.2.1/0.2.2 | Warm palette работает; cool palette (`palette_2b.json`) всё ещё ждёт белые/чёрные/нейтрально-серые декорации |
| KI-3..KI-8 | fixed 0.2.0 | `color_metric`, `dither`, alpha, `jitter`, `bounds`, `--preview` |
| KI-9 | fixed 0.2.1 | `is_art` резолвится через hash, не через name |
| KI-10 | new 0.2.3 | Каталог размеров измеряет placement footprint, не sprite bounds — см. ниже |

### KI-2 (обновлено в 0.2.2)

В 0.2.2 добавлены 18 новых декораций Canal Hideout, включая **4 marble**
серии (Bench/Table/Walls/Fountain) и **Cave Fossil** — кандидаты на
«белый/светло-серый» для холодных палитр. Точные RGB пока не измерены;
пользователь должен сделать скриншоты в игре и присылать — добавлю в
`palette_2b.json` и закрою KI-2 полностью.

### KI-10 (новое в 0.2.3) — каталог размеров: placement vs sprite bounds

`DECORATION_FOOTPRINT_CATALOG` основан на минимальной парной евклидовой
дистанции между same-hash размещениями в `исходники/*.hideout`. Это
**верхняя граница placement-тайла**, а не реальные sprite bounds:

- Видимая крона дерева выходит за пределы placement-тайла.
- Все наблюдения сделаны при `r=0`; повёрнутые размещения могут иметь
  другие эффективные границы.
- Для `img2hideout` step-калибровки placement footprint — то, что нужно.
  Для детекции визуального перекрытия потребуются внутриигровые замеры
  sprite (из `.hideout` файлов не выводятся).

Дополнительно: для 4 декораций (Marble Bench, Marble Walls, Marble
Fountain, Petrified Cave Figure) в исходниках всего по 1 размещению —
измерить невозможно, `confidence=single`. Для Marble Table — 2
размещения, `min=45.6 wu → 2.0 tile`, но это `confidence=low`: единственная
пара могла оказаться далеко друг от друга по совпадению, а не потому что
стол реально 2 тайла шириной.

**Закроется**, когда: появится multi-pass рендер с использованием каталога
для запрета размещения крупных декораций рядом с соседями, либо
пользователь пришлёт отдельный скриншот с замером реального sprite bounds.

## Что улучшать дальше (не в этой итерации)

- **Multi-pass: outline + fill.** Сначала рисуем контур, потом заливку —
  узнаваемость фигур сильно вырастет. Реализуется в `img2hideout.py`
  через дополнительный параметр `outline_color` в палитре +
  двухпроходной рендер. Естественным образом использует
  `DECORATION_FOOTPRINT_CATALOG`: на контурном проходе размещаем только
  small-декорации (1 тайл), на fill — средние/крупные с учётом размера.
- **Точные RGB для холодных декораций.** Marble-серия и Cave Fossil —
  кандидаты на white/light-gray для `palette_2b.json`. Пользователь
  присылает скриншот — измеряю median RGB и заполняю TODOs. Закрывает KI-2.
- **Замер sprite bounds.** Расширить каталог полем `sprite_bounds_wu`
  (отдельно от placement footprint), когда появятся внутриигровые замеры
  или VLM-анализ скриншотов. Закрывает KI-10.
- **Поддержка нескольких убежищ.** Сейчас `NAMED_BOUNDS` содержит только
  `canal`. Добавить новые записи по мере появления экспортов других
  убежищ (с пользовательской разметкой границ по образцу).
- **SVG / Lua экспорт.** Для пользователей, которые хотят ручной доводки.

## Базовые принципы при правках

1. **Сначала в STATUS.md, потом фикс.** Новый баг → новый KI-N → потом код.
2. **Никогда не ломать существующие тесты.** Все новые опции — opt-in,
   дефолты сохраняют текущее поведение.
3. **Никогда не выдумывать хеши.** Нет в `KNOWN_HASHES` → помечаем как
   unknown, не подменяем.
4. **Документация короткая.** Этот файл — не больше 100 строк.
5. **Каталог размеров — sync с исходниками.** При добавлении новых
   размещений в `исходники/` перезапускать `scripts/measure_decorations.py`
   и обновлять `DECORATION_FOOTPRINT_CATALOG`. Тест
   `test_sample_counts_match_real_exports` упадёт, если рассинхрон.
